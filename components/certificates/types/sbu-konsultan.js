import { renderSbuUserForm } from '../shared/forms.js';

export function renderSbuKonsultanForm(submission, certificate, appState) {
    return renderSbuUserForm(submission, certificate, appState);
}
