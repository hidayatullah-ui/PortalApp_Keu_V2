import { renderSimpleCertForm } from '../shared/forms.js';

export function renderNotarisForm(submission, certificate, appState) {
    return renderSimpleCertForm(submission, certificate, appState);
}
